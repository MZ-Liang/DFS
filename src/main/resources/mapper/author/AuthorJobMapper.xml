<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dfs.api.mapper.author.AuthorJobMapper">

  <sql id="select_column">
    id, author_id, job_id
  </sql>

  <sql id="where_condition">
    <where>
      <if test="authorId != null">
        author_id = #{authorId,jdbcType=BIGINT}
      </if>
      <if test="jobId != null">
        and job_id = #{jobId,jdbcType=BIGINT}
      </if>
    </where>
  </sql>

  <insert id="insert" parameterType="com.dfs.api.entity.author.AuthorJobEntity">
    insert into author_job (id, author_id, job_id
      )
    values (#{id,jdbcType=BIGINT}, #{authorId,jdbcType=BIGINT}, #{jobId,jdbcType=BIGINT}
      )
  </insert>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from author_job
    where id = #{id,jdbcType=BIGINT}
  </delete>

  <update id="updateByPrimaryKey" parameterType="com.dfs.api.entity.author.AuthorJobEntity">
    update author_job
    set author_id = #{authorId,jdbcType=BIGINT},
      job_id = #{jobId,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <update id="updateByPrimaryKeySelective" parameterType="com.dfs.api.entity.author.AuthorJobEntity">
    update author_job
    <set>
      <if test="authorId != null">
        author_id = #{authorId,jdbcType=BIGINT},
      </if>
      <if test="jobId != null">
        job_id = #{jobId,jdbcType=BIGINT},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultType="com.dfs.api.entity.author.AuthorJobEntity">
    select 
    <include refid="select_column" />
    from author_job
    where id = #{id,jdbcType=BIGINT}
  </select>

  <select id="selectByCondition" parameterType="com.dfs.api.entity.author.AuthorJobEntity" resultType="com.dfs.api.entity.author.AuthorJobEntity">
    select 
    <include refid="select_column" />
    from author_job
    <include refid="where_condition" />
  </select>

  <select id="countByCondition" parameterType="com.dfs.api.entity.author.AuthorJobEntity" resultType="java.lang.Integer">
    select count(*) 
    from author_job
    <include refid="where_condition" />
  </select>
</mapper>
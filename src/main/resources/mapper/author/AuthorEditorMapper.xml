<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dfs.api.mapper.author.AuthorEditorMapper">

  <sql id="select_column">
    id, author_id, editor_id, edit_type
  </sql>

  <sql id="where_condition">
    <where>
      <if test="authorId != null">
        author_id = #{authorId,jdbcType=BIGINT}
      </if>
      <if test="editorId != null">
        and editor_id = #{editorId,jdbcType=BIGINT}
      </if>
      <if test="editType != null">
        and edit_type = #{editType,jdbcType=INTEGER}
      </if>
    </where>
  </sql>

  <insert id="insert" parameterType="com.dfs.api.entity.author.AuthorEditorEntity">
    insert into author_editor (id, author_id, editor_id, 
      edit_type)
    values (#{id,jdbcType=BIGINT}, #{authorId,jdbcType=BIGINT}, #{editorId,jdbcType=BIGINT}, 
      #{editType,jdbcType=INTEGER})
  </insert>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from author_editor
    where id = #{id,jdbcType=BIGINT}
  </delete>

  <update id="updateByPrimaryKey" parameterType="com.dfs.api.entity.author.AuthorEditorEntity">
    update author_editor
    set author_id = #{authorId,jdbcType=BIGINT},
      editor_id = #{editorId,jdbcType=BIGINT},
      edit_type = #{editType,jdbcType=INTEGER}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <update id="updateByPrimaryKeySelective" parameterType="com.dfs.api.entity.author.AuthorEditorEntity">
    update author_editor
    <set>
      <if test="authorId != null">
        author_id = #{authorId,jdbcType=BIGINT},
      </if>
      <if test="editorId != null">
        editor_id = #{editorId,jdbcType=BIGINT},
      </if>
      <if test="editType != null">
        edit_type = #{editType,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultType="com.dfs.api.entity.author.AuthorEditorEntity">
    select 
    <include refid="select_column" />
    from author_editor
    where id = #{id,jdbcType=BIGINT}
  </select>

  <select id="selectByCondition" parameterType="com.dfs.api.entity.author.AuthorEditorEntity" resultType="com.dfs.api.entity.author.AuthorEditorEntity">
    select 
    <include refid="select_column" />
    from author_editor
    <include refid="where_condition" />
  </select>

  <select id="countByCondition" parameterType="com.dfs.api.entity.author.AuthorEditorEntity" resultType="java.lang.Integer">
    select count(*) 
    from author_editor
    <include refid="where_condition" />
  </select>
</mapper>